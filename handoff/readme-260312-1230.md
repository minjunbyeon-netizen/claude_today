# Handoff - daily-focus - 2026-03-12 12:30

## Completed
- Fixed "계속" button error: `wt` not found (0x80070002)
  - Root cause: server runs as background process without WindowsApps in PATH
  - Fix: use absolute path `C:\Users\USER\AppData\Local\Microsoft\WindowsApps\wt.exe`
- Improved force-focus PowerShell script with AttachThreadInput trick
  - Prevents Windows blocking SetForegroundWindow from background processes
  - Steps: ShowWindow(MAXIMIZED) → AttachThreadInput → BringWindowToTop → SetForegroundWindow

## Remaining
- None

## Context
- WT_EXE = `C:\Users\USER\AppData\Local\Microsoft\WindowsApps\wt.exe` (absolute path)
- CLAUDE_CMD = `C:\Users\USER\AppData\Roaming\npm\claude.cmd` (unchanged)
- Focus script: GetForegroundWindow → GetWindowThreadProcessId → AttachThreadInput → BringWindowToTop
- Server restart: `powershell.exe -Command "Stop-Process -Id <PID> -Force"`
