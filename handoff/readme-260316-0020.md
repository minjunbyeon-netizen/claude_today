# Handoff - daily-focus - 2026-03-16

## Completed
- Sessions panel: idle entries fully hidden; stale (>60min) shown at 45% opacity with gray dot
- Morning board: padding reduced (18→14px), pills moved inline next to title, headline compact, layout gap reduced, metric cards smaller, goal input height reduced (48→36px), prompt font smaller

## Remaining
- None

## Next Steps
- Reload page to verify visual changes

## Context
- Sessions filter: `rows.filter(r => r.status !== 'idle')` — all idle removed regardless of source
- Stale threshold changed from 30min to 60min for sessions panel
- morning-top now uses flexbox row with `.morning-top-left` wrapper containing title + headline + pills inline
