# Handoff - daily-focus - 2026-03-17 18:30

## Completed
- Full DB persistence audit
- Fixed: carry_over() now calls log_activity() — user_activity will accumulate going forward
- CC button fixed: wt.exe removed, using cmd CREATE_NEW_CONSOLE + window reactivation by title
- Calendar full-month dots: loadCalendarActivity() in initApp()
- db-audit.html visualization created

## Audit Results — All Clear / Issues

### OK
- tasks: 419 records (todo 83, done 298, carried_over 38)
- agent_activity: 1,139 CC session events across all projects
- settings: 12 keys (ui_prefs, wf_groups, telegram config slots, token limits)
- localStorage: 0 references (fully migrated to DB)
- ui_prefs in DB: active_tab, claude_open, acc_open, proj_order, card_states, wf_collapsed, wfg_states
- wf_groups: 5 groups stored in DB
- task_learning_samples: 298 records
- workspace_projects: 30 projects auto-detected

### Fixed This Session
- user_activity was empty: carry_over() didn't call log_activity() → fixed
- CC button 0x80070002: removed wt.exe, use cmd + CREATE_NEW_CONSOLE

### Remaining Issues
- checkins, weekly_goals: tables exist but no UI connected (low priority)
- local DB vs remote (137.184.82.157) are separate instances — no sync
- telegram not configured (settings key missing)

## Next Steps
- Confirm CC button works (test with a project)
- Consider: local-only or remote-only operation to avoid DB split
- Optional: configure Telegram for EOD auto-report

## Context
- DB: data/focus.db
- Server: port 8001
- Remote URL: http://137.184.82.157/daily-focus (separate DB instance)
- carry_over fix: app.py ~line 3112
- CC launch fix: app.py:3426 open_claude()
