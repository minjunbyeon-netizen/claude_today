# Handoff - daily-focus - 2026-03-16

## Completed
- Added `renderPastDayView(tasks)` function: groups tasks by project, shows done/total count per group, lists tasks with simple strikethrough for done items
- Modified `loadTodayByDate`: detects past day (`ds < todayStr()`) and routes to `renderPastDayView` instead of `renderAccordion`
- Modified `selectDate`: hides `#addTaskCard` (add task form) when navigating to a past day, restores it for today
- Added CSS `.past-view*` classes for the simple read-only layout
- Added `id="addTaskCard"` to the add task card div for show/hide control

## Remaining
- None

## Next Steps
- Test by clicking past dates in the calendar — should show simple project-grouped task list
- Today's date still shows full accordion with all controls

## Context
- Past day detection: `ds < todayStr()` string comparison works because date format is YYYY-MM-DD
- `applyProjectFilter` (project filter mode) is unaffected — still uses renderAccordion even for past days when a project filter is active
