# Handoff - daily-focus - 2026-03-17 16:50

## Completed
- WF groups (프로젝트 폴더 그룹) localStorage → 서버 DB 마이그레이션
  - API 추가: GET /api/wf-groups, POST /api/wf-groups
  - settings 테이블에 wf_groups 키로 JSON 저장
  - JS 변경: _wfGroupsCache 인메모리 캐시 + loadWfGroupsFromServer() 함수
  - saveWfGroups() → 로컬캐시 업데이트 + 서버 비동기 POST
  - 앱 초기화 시 loadWfGroupsFromServer() → 서버 우선, 없으면 localStorage 마이그레이션 자동 실행
  - getWfGroups() 동기 유지 → 기존 코드 전부 무수정

## Behavior
- 기존 localStorage에 WF 그룹이 있으면 최초 로드 시 서버로 자동 업로드
- 이후 모든 저장은 서버 DB 우선
- 서버 장애 시 localStorage 캐시 폴백

## Remaining
- @mention feature (MAJOR — deferred)
- CommentPanel unification (deferred)

## Context
- WF groups data structure: [{id, name, folders: [projectName, ...]}]
- Stored in settings table with key 'wf_groups'
- 오늘 tasks 사라진 원인: tasks는 DB에 정상 있음 (4개 carry-over). 추가 조사 불필요.
