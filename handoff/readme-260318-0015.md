# Handoff - daily-focus - 2026-03-18 00:15

## Completed
- 작업 탭: 태스크 없는 폴더에 "태스크 없음" 대신 CC 마지막 보고 이력 3줄 표시
- Backend: GET /api/agent-status 응답에 `recent_activity: [{task, status, created_at}]` 필드 추가 (agent_activity 테이블에서 프로젝트별 최근 3개)
- Frontend: `agentLastActivity` 맵 선언 + loadAgentStatus()에서 채움
- `buildLastActivityRows(proj)` 함수: 최신 1행은 bold+불투명, 나머지 2행은 연하게
- 상태별 색상 dot: done=녹색, start=파랑, step=회색, warn=주황
- 시간 표시: "방금/N분 전/N시간 전/N일 전"

## Data Flow
- agent_activity 테이블 → /api/agent-status `recent_activity` 필드 → `agentLastActivity[project]` → `buildLastActivityRows(proj)` → folder row HTML

## Context
- activity 없는 폴더는 기존 "태스크 없음" 텍스트 유지
- `agentLastActivity` is module-level const, updated every 60s via loadAgentStatus()
- CSS: .cc-act-row, .cc-act-dot-*, .cc-act-task, .cc-act-latest
