# Handoff - daily-focus - 2026-03-17 18:15

## Completed
- CC 버튼 launch 방식 전면 교체 (wt.exe → cmd.exe)
  - 기존: wt.exe + claude.exe 직접 경로 → 0x80070002 오류
  - 신규: `subprocess.Popen(['cmd', '/k', 'title CC:{proj} & claude'], cwd=target_path, creationflags=CREATE_NEW_CONSOLE)`
  - 이미 실행 중인 창 감지: PowerShell로 `MainWindowTitle -like 'CC:{proj}*'` 검색 → SetForegroundWindow
  - claude는 PATH에서 자동 검색 (npm 설치된 claude.cmd 사용)
- Calendar full-month dots now load on page init (loadCalendarActivity in initApp)

## Remaining
- @mention feature (deferred)
- CommentPanel unification (deferred)
- sync_workspace_projects() overwrites custom folder_path (identified, not fixed)

## Next Steps
- Test CC button on a project → should open cmd window titled "CC:{proj}" with claude running
- Second click → should bring existing window to front (reactivation)

## Context
- open_claude endpoint: app.py:3426
- Window title tag format: "CC:{proj}" (e.g. "CC:daily-focus")
- CREATE_NEW_CONSOLE flag opens independent visible cmd window in taskbar
- claude.exe at C:\Users\USER\.local\bin\claude.exe (also in npm)
- DB: data/focus.db, server port 8001
