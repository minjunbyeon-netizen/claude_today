# Handoff - daily-focus - 2026-03-19 16:00

## Completed
- 사이드바 프로젝트 섹션에 3단계 계층 추가 (카테고리 > 그룹 > 폴더)
- 각 그룹에 `category` 필드 추가 (선택, 빈 값이면 기존과 동일하게 flat 표시)
- `renderProjectList`: 카테고리 있는 그룹은 카테고리 헤더 아래 묶어 표시, 클릭으로 접기/펼치기
- 설정 모달 각 그룹 블록에 "카테고리" 입력 필드 추가
- `setCategoryForGroup` 함수 추가, `toggleCat` / `_buildProjItem` 헬퍼 추가
- `.proj-cat-label` CSS를 C-08 규칙에 맞게 수정 (92% 흰색, bold, uppercase 제거)

## Remaining
- None

## Next Steps
- 설정 모달에서 각 그룹 오른쪽 "카테고리" 입력칸에 카테고리명 입력하면 사이드바에 반영
- 같은 카테고리명을 입력한 그룹들이 자동으로 묶임

## Context
- Data structure: group `{ id, name, folders[], category? }` — category is optional string
- Uncategorized groups (category empty) render flat as before (backward compatible)
- Categorized groups render under collapsible `proj-cat-hd` header
- `_collapsedCats`: in-memory Set, resets on page reload
- CSS: `.proj-item.cat-item` adds `padding-left: 16px` for indentation inside category
- All in `static/index.html` only (no backend changes needed — category stored in wf-groups JSON)
