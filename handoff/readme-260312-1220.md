# Handoff - daily-focus - 2026-03-12 12:20

## Completed
- Fixed Claude usage display: replaced single monthly-token bar with 3 plan rate limit bars
  - 현재 세션 (5h window): session_pct = session_tokens / session_token_limit
  - 주간 모든 모델 (7d window): weekly_pct = weekly_tokens / weekly_token_limit
  - Sonnet만 (7d window): weekly_sonnet_pct = weekly_sonnet_tokens / weekly_sonnet_limit
- Set plan limits in DB (settings table):
  - session_token_limit = 450,000,000 (5h window)
  - weekly_token_limit = 3,000,000,000 (7d window)
  - weekly_sonnet_limit = 2,120,000,000 (7d Sonnet, derived from screenshot 82%)
- Added reset time display: oldest-token-in-window + window-duration - now = reset_min
- Fixed agent-status GET endpoint to merge JSONL-detected sessions (24h window)
  - Projects with JSONL activity in last 24h but no DB hook report now appear in sessions panel
  - Deduplication via normalized name matching (hyphens/underscores treated as same)
- Debugged 5 scenarios: all PASS

## Remaining
- None

## Next Steps
- Check dashboard in browser to verify 3-bar layout matches screenshot
- If plan limits become inaccurate over time, update settings: session_token_limit, weekly_token_limit, weekly_sonnet_limit

## Context
- `/api/claude-usage` in app.py: now scans ALL JSONL (not just month_prefix) for session/weekly windows
  - Uses UTC timestamps with timezone-aware datetime comparison
  - session_total = 5h window, weekly_total = 7d window, weekly_sonnet = 7d Sonnet-only
  - Defaults in code: 450M/3B/2.12B, but actual values come from settings table
- `/api/agent-status` GET: merges DB rows with JSONL file mtime detection
  - from_jsonl=True flag on JSONL-detected entries
  - normalized match: hyphens/underscores removed before comparison
- UI: claude detail section now shows 3 bars (session / weekly / sonnet) + existing monthly bar
- Server restart method: use PowerShell Stop-Process (taskkill /PID doesn't work in Git Bash)
  - keepalive.py auto-restarts: Stop-Process -Id <PID> -Force
