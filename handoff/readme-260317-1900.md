# Handoff - daily-focus - 2026-03-17 19:00

## Completed
- CC activation detection verified working:
  - _is_claude_active() correctly returns ACTIVE for daily-focus, chunking-english
  - PowerShell SetForegroundWindow on WindowsTerminal: activated PID=33304
- Widget launch button added to titlebar (top-right):
  - Button: grid icon + "WIDGET" label, dark translucent style
  - POST /api/launch-widget endpoint added to app.py
  - Logic: check electron process with MainWindowHandle!=0 → bring to front
  - If no widget running: launch widget-start-silent.vbs (silent, no cmd popup)
  - Button feedback: shows "FOCUSED" or "LAUNCHED" for 2s then resets
- Widget verified: VBS launches Electron, PID=37560, MainWindowHandle=591872, Title="Daily Focus CC / VIEW"
- Sync: widget loads same http://localhost:8001 → real-time sync automatic

## Architecture
- Widget = Electron wrapper around http://localhost:8001
- No separate sync needed — same server, same DB
- Widget is always-on-top, frameless, 460x780px by default

## Remaining
- @mention, CommentPanel (deferred)
- local vs remote DB split
- Telegram not configured

## Context
- launch-widget endpoint: app.py before _is_claude_active (~line 3427)
- WIDGET_VBS path: C:\work\daily-focus\widget\widget-start-silent.vbs
- Widget button: index.html titlebar-right, before 댓글 badge
- launchWidget() JS: index.html ~line 2854 (inside <script> block)
