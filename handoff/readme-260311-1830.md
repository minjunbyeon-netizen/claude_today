# Handoff - daily-focus - 2026-03-11 18:30

## Completed
- Unified all polling intervals to 60s (AUTO_REFRESH_MS, loadClaudeUsage, loadAgentStatus)
- Force pushed local version over remote (Work Radar variant discarded, Daily Focus CC kept)
- Investigated ops-brief button slowness: root cause = collect_repo_snapshots() runs git status+log on 14 repos (~1.4s cold)
- Cache TTL increased 20s → 60s for repo snapshots and workspace projects
- Fixed button UX: optimistic update (instant fade+remove item on click, background reload)
- Added data-task-id attribute to ops-item elements for DOM targeting
- All commits pushed to GitHub master

## Remaining
- Server cache still shows ~2s every request even after warmup (cache not hitting in server process)
  - This needs deeper investigation: possibly concurrent requests or async thread pool issue
- Could further optimize by pre-warming cache on server startup

## Next Steps
- If 2s server-side slowness is still a problem: investigate why _REPO_SNAPSHOT_CACHE doesn't persist between requests in uvicorn
- Consider running collect_repo_snapshots() in a background thread on startup to pre-warm cache
- Or increase cache TTL to 300s (5 min) to reduce cold cache frequency

## Context
- DB: C:/work/daily-focus/data/focus.db (daily_focus.db in root is empty/unused)
- Server PID: 36332 (started with: python -m uvicorn app:app --host 0.0.0.0 --port 8001)
- run.py uses Windows mutex singleton - use uvicorn directly if run.py says "already running"
- REPO_SCAN_ROOTS = [C:\work, C:\Users\USER\Desktop] - 14 git repos total
- Button optimistic update: ops-item has data-task-id, clicked item fades instantly, POST happens async
