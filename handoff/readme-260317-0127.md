# Handoff - daily-focus - 2026-03-17 01:27

## Completed
- Rewrote renderGrid to support group-sectioned view:
  - currentGroupId === null (전체): renders all configured groups as full-width section headers with sub-grids of folder cards
  - currentGroupId set: renders only that group's folder cards in a flat grid
- Added CSS: .grid-section-hd (group label header) and .grid-section-cards (sub-grid container)
- Project sidebar now shows only 전체 + configured groups (no individual folders, no 미분류)
- selectGroup() auto-switches to grid view and calls renderGrid with group context
- Live folders sorted first within each group section

## Remaining
- None

## Next Steps
- Test grid group sections with real data (configure groups via settings, verify cards appear per group)
- If user wants: add "empty group" placeholder text when a group has no active tasks

## Context
- df_wf_groups localStorage key stores group config: [{id, name, folders:[...]}]
- getWfGroups() reads this key; folders are normalized folder names matching task [bracket] prefixes
- renderGrid container switches between display:block (전체 sectioned) and display:grid (single group)
- PM2 manages server: use `pm2 restart daily-focus` only — never restart_server.py or direct uvicorn
