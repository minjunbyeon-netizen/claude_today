# Handoff - daily-focus - 2026-03-17 15:00

## Completed
- Fixed "object Object / 경로를 찾을 수 없음" error in ops brief "이어서 시작" button
  - Root cause: `renderOpsBrief` called `launchBriefItem(cmd)` where cmd is an object
  - `launchBriefItem(project, prompt)` expects two strings; `encodeURIComponent(cmd)` → `[object%20Object]`
  - Fix: `launchBriefItem(cmd.project, cmd.launch_prompt)` (index.html line 3520)
- Fixed CC button 0x80070002 error (persistent even after Start-Process wt fix)
  - Root cause: `Start-Process wt` in pm2's PowerShell environment cannot resolve App Execution Alias
    (App Execution Aliases require user shell session context — pm2 runs without it)
  - Fix: replaced with `os.startfile("wt: new-window -d ... -- cmd /k ...")` which uses
    `ShellExecuteEx` — always has user session context, resolves `wt:` URI scheme reliably
- Committed f4a00a4, pm2 restarted

## Remaining
- @mention feature (MAJOR — deferred, high complexity)
- CommentPanel unification across 4 views (structural refactor, deferred)
- End-of-day report auto-generation
- Telegram briefing integration
- Project health score
- WARN-03: folder-view wf-cc button missing loading state

## Next Steps
- Test CC button: click CC on project card → verify Windows Terminal opens in correct dir with claude
- Test "이어서 시작": ops brief command section → click button → verify correct project + prompt passed
- Test openCC from group folder view (line 2875)

## Context
- All CC launch paths call /api/open-claude → open_claude() → os.startfile("wt: ...")
- `wt:` URI scheme is registered by Windows Terminal installation
- If wt: URI fails (WT not installed), os.startfile raises OSError with informative message
- lotto-app IS in C:\work\ and IS found by find_repo_for_project("lotto-app") → key "lottoapp"
- Workspace scan root: C:\work, WORKSPACE_SCAN_SKIP_DIRS excludes common non-project dirs
- Previous commits: fa0f8ee (Start-Process wt — partially worked), c225b80 (comment UX), 1cfc3ae (Strategic Advisor)
