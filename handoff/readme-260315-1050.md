# Handoff - daily-focus - 2026-03-15 10:50

## Completed
- Fixed `background_start.bat`: added `-WindowStyle Hidden` to PowerShell call (was showing a window at startup)
- Fixed `background_start.bat`: changed `Start-Process -WindowStyle Minimized` to `Start-Process -WindowStyle Hidden` (minimized still steals focus)
- Added `windowsHide: true` to PM2 ecosystem.config.js zombie-watchdog entry
- Removed `pause` from `run_weekly.bat` in HivemediaWeeklyTrend scheduled task (was keeping CMD window open)
- Created `C:/work/setting/zombie/run_hidden.vbs` as a VBS wrapper for silent execution
- Restarted zombie-watchdog and saved PM2 config

## Remaining
- None

## Next Steps
- Monitor if terminal windows still appear after these fixes
- If still occurring, check: Windows Snipping Tool shortcut settings, any other scheduled tasks, Codex background tasks

## Context
- The user was seeing terminal/CMD windows popping up and interrupting work
- Root causes identified:
  1. `background_start.bat` (Startup folder) → PowerShell without -WindowStyle Hidden
  2. PM2 zombie-watchdog (every 10min) → no windowsHide:true flag
  3. HivemediaWeeklyTrend scheduled task → run_weekly.bat had `pause` keeping window open
- Screenshots on Desktop named "YYYY-MM-DD HH MM SS.png" are manually taken by the user via a screenshot tool, not auto-created by any script
