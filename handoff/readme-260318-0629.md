# Handoff - daily-focus - 2026-03-18 06:29

## Completed
- KST timezone 전면 수정 (app.py + index.html)
- 120 시나리오 버그 5건 픽스 (S-34, S-68, S-100, S-113, S-117)
- 과거 날짜 캘린더 클릭 시 작업 목록 표시 정상화
- "오늘로 이월" carryBanner class="banner" 추가
- watchdog + Windows 자동시작 등록

## 핵심 수정 내용 (KST)

### app.py
- `KST = timezone(timedelta(hours=9))` 상수 추가
- `kst_today()`, `kst_now()`, `kst_now_dt()` 헬퍼 함수 추가
- `str(date.today())` 전체 → `kst_today()` 교체 (20+건)
- `datetime.now().isoformat()` 전체 → `kst_now()` 교체
- 시간 연산 관련 `datetime.now()` → `kst_now_dt()` 교체

### index.html
- `selectDate()`, `switchSubTab()`, `updateMainHeader()` → `todayStr()` 대신 `serverToday` 사용
- `loadTodayByDate()` → `isToday / isPastDay` 모두 `serverToday` 기준
- `loadToday() / refreshMain()` → `serverToday` 기준
- `loadWeekTaskMap()` → `server_today` 수신 시 `currentViewDate` 자동 보정

## Remaining
- None

## Next Steps
- 드롭렛 배포 완료 후 KST 날짜 정상 동작 확인

## Context
- 문제의 근본: 드롭렛은 UTC 서버 → 06:22 KST = 전날 21:22 UTC → date.today() = 3월17일
- 클라이언트 todayStr() = 3월18일 vs 서버 server_today = 3월17일 불일치가 모든 날짜 버그 원인
- kst_today() = (UTC+9 기준 오늘) 이므로 서버 timezone 무관하게 항상 KST 날짜 반환
