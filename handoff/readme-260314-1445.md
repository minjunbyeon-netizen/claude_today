# Handoff - daily-focus - 2026-03-14 14:45

## Completed
- CC usage value font shrunk (26px -> 18px), label shortened to "잔여량"
- 자동감시 card removed; watch dot + "Xs 감시" text merged inline into CC header
- Morning board replaced: now shows per-project breakdown (numbered list)
  - Each row: project name / yesterday done / today remaining / "허용" button
  - "허용" button fills the goal input and saves immediately
- loadMorningProjectBoard() fetches today + yesterday tasks, groups by [project] prefix

## Remaining
- None

## Next Steps
- Check if morningHeadline text still shows old garbled text — may need to update the JS that sets it
- Consider refreshing morningProjectBoard on autoRefreshTick

## Context
- mpb-* CSS classes handle the project board rows
- addProjectToGoal(proj, task) posts to /api/today-goal with confirm:true
- watchDotInline / watchStatusInline are the two new inline elements in CC header
