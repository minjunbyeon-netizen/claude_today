# Handoff - daily-focus - 2026-03-17 18:45

## Completed
- CC activation detection completely rewritten: JSONL file mtime based
  - Root cause: CC sessions run inside Windows Terminal tabs, not standalone cmd windows
    - cmd.exe has MainWindowHandle=0 (no visible window on its own)
    - Window title search was fundamentally wrong
  - New detection: _is_claude_active(proj) checks ~/.claude/projects/C--work-{proj}/*.jsonl mtime within 10 min
  - Tested: daily-focus=ACTIVE, chunking-english=ACTIVE, others=inactive — all correct
  - On active: bring WindowsTerminal window to foreground (SetForegroundWindow)
  - On inactive: open new cmd window (CREATE_NEW_CONSOLE) with claude

## Process Tree (how CC runs)
- User opens WT tab → cmd.exe (no window handle) → node.exe (claude cli.js)
- node.exe writes to ~/.claude/projects/C--work-{proj}/{session-id}.jsonl continuously
- 4 active node processes detected: daily-focus, chunking-english + 2 more

## Remaining
- @mention, CommentPanel (deferred)
- local vs remote DB split (no sync)
- Telegram not configured

## Context
- _is_claude_active(): app.py before open_claude endpoint (~line 3427)
- JSONL folder naming: C--work-{project-name} (path separators become --)
- Cutoff: 10 minutes (if no JSONL write in 10 min, considered inactive)
- WindowsTerminal PID: one process hosts all tabs — bringing it to front shows last active tab
