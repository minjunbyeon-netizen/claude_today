# Handoff - daily-focus - 2026-03-12 11:25

## Completed
- Fixed "계속" button: now opens Claude Code (Windows Terminal new tab) for the task's project
- Added `continueAndLaunch(taskId, project, prompt)` JS function in index.html
- Expanded `can_open_claude` to include workspace projects (not just git repos) in app.py
- "계속" button now styled in blue to visually distinguish from other decision buttons
- TEXT button removed from separate display (redundant, 계속 now does same job)

## Remaining
- "보류", "이월", "분해" buttons still only do DB updates (no Claude Code launch) - this may be intentional
- Verify that `api/open-claude` successfully opens Windows Terminal for workspace-only projects

## Next Steps
1. Test by clicking "계속" button in the browser - should open a new Windows Terminal tab with Claude Code
2. Verify the project name resolution works for daily-check-stage-* folders
3. If "새 폴더 감지 - 목표와 첫 작업 정의" tasks are noise, consider adding auto-dismiss logic

## Context
- `continueAndLaunch` calls `api/open-claude?proj=...&prompt=...` directly
- `api/open-claude` uses Windows Terminal (`wt new-tab`) + PowerShell focus
- `can_open_claude` now: `bool(repo_snapshot) or bool(_task_project and find_repo_for_project(...))`
- `find_repo_for_project` checks git repos first, then workspace_projects table
- Server auto-restarts via keepalive.py when killed
