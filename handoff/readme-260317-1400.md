# Handoff - daily-focus - 2026-03-17 14:00

## Completed
- Fixed CC button 0x80070002 (ERROR_FILE_NOT_FOUND) error
  - Root cause: WT_CANDIDATES included `WindowsApps\wt.exe` (App Execution Alias stub)
    which `os.path.exists()` reports as existing but PowerShell subprocess (shell=False) cannot execute
  - Fix: removed WT_CANDIDATES/WT_EXE path detection entirely
  - Now uses `Start-Process wt -ArgumentList @(...)` in PowerShell — shell resolves App Execution Alias correctly
  - Fallback to cmd.exe new console if PowerShell itself is not found
- Committed and pushed (fa0f8ee)

## Remaining
- @mention feature (MAJOR — deferred, high complexity)
- CommentPanel unification across 4 views (structural refactor, deferred)
- End-of-day report auto-generation
- Telegram briefing integration
- Project health score
- WARN-01: Korean path + cmd.exe fallback CP949 collision (low priority)
- WARN-03: folder-view wf-cc button missing loading state

## Next Steps
- Restart server: pm2 restart daily-focus (already done)
- Test CC button: click CC on any project card, verify Windows Terminal opens with claude in correct dir
- If wt not in PATH via Start-Process, test cmd.exe fallback path

## Context
- App Execution Alias stubs live in `C:\Users\USER\AppData\Local\Microsoft\WindowsApps\`
  They exist on disk (os.path.exists=True) but are NOT real executables — they are OS-managed stubs
  that only work when launched through the Windows shell, not via direct subprocess exec
- `Start-Process wt` lets PowerShell resolve the alias through the shell layer — this is the correct pattern
- Previous LIVE/comment UX work committed at c225b80
- Strategic Advisor panel committed at 1cfc3ae
