# Handoff - daily-focus / VPS mcp deployment - 2026-03-13 12:55

## Completed
- Step 1: pip install log checked - was still running (killed mid-way by OOM)
- Step 2: Flask confirmed installed at /home/dev/work/mcp/.venv/lib/python3.12/site-packages/flask/
- Step 3: flask-cors was corrupted after OOM kill; force-reinstalled flask, flask-cors, authlib, python-dotenv successfully
- Step 4: app.py launched successfully (HTTP:200 confirmed while running)
- Step 6: systemd service installed, enabled, and started - `mcp.service` Active: active (running) PID 152343
- Step 7: nginx include added at line 77 of /etc/nginx/sites-enabled/work (NOT "default"); `nginx -t` passed; nginx reload command was issued

## Remaining
- Confirm nginx was actually reloaded (sudo systemctl reload nginx may have failed silently)
- Confirm Step 8: `curl -s http://localhost/mcp/ -o /dev/null -w '%{http_code}'` returns 200
- VPS SSH is currently unresponsive (sshd likely OOM-killed); requires reboot via DigitalOcean control panel or waiting for sshd to auto-restart

## Next Steps
1. Reboot VPS via DigitalOcean console if SSH remains unavailable
2. After SSH is back: run `sudo systemctl reload nginx` if not already done
3. Run: `curl -s http://localhost/mcp/ -o /dev/null -w '%{http_code}'` to confirm nginx proxy works
4. Run: `curl -s http://137.184.82.157/mcp/` to confirm public access works

## Context
- VPS: 137.184.82.157, user: dev, SSH alias: vps-dev
- VPS specs: 1 vCPU, 961MB RAM, NO SWAP - very tight on memory
- MySQL is consuming ~39-40% RAM (392MB) - main memory pressure source
- mcp app runs on port 5051 (Flask)
- nginx config file: /etc/nginx/sites-enabled/work (NOT default)
- nginx-mcp.conf inserted before `location /nara/` at line 77
- mcp.service path: /etc/systemd/system/mcp.service
- venv path: /home/dev/work/mcp/.venv (python -> /usr/bin/python3)
- logs: /home/dev/work/mcp/logs/web-app.{out,err}.log
- The VPS has no swap - OOM kills are frequent under load; consider adding swap or reducing MySQL memory
