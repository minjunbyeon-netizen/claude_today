# Handoff - daily-focus - 2026-03-12 11:30

## Completed
- Changed `wt new-tab` to `wt new-window` so Claude Code always opens in a fresh, visible window
- Changed PowerShell focus script: SW_SHOWMAXIMIZED (3) instead of SW_RESTORE (9)
- Added Sort-Object StartTime to target the NEWEST WindowsTerminal process
- Increased pre-focus delay from 0ms to 600ms to let the window fully render before maximizing

## Remaining
- None for this task

## Next Steps
- Click "계속" in the dashboard and verify new maximized WT window appears immediately
- If still not visible enough, consider using SetWindowPos to position to primary monitor center

## Context
- open_claude endpoint at app.py line ~2945
- PowerShell: SW_SHOWMAXIMIZED = 3, SW_RESTORE = 9
- Server auto-restarts via keepalive.py after kill
