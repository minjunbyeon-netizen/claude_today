# Handoff - daily-focus - 2026-03-16 18:30

## Completed
- Added task_comments table: id, task_id, author, body, confirmed, confirmed_by, confirmed_at, created_at
- Added API endpoints:
  - GET /api/me → current logged-in user info
  - GET /api/tasks/{id}/comments → list comments for task
  - POST /api/tasks/{id}/comments → post comment (author = session login)
  - PATCH /api/tasks/{id}/comments/{cid}/confirm → mark confirmed (strikethrough)
- Frontend: comment button (💬) added to each task action bar
- Comment panel toggles open/close per task, lazy-loads comments
- Confirmed comments show strikethrough + ✓ mark
- Enter key sends comment

## Remaining
- None

## Next Steps
- Test with team member: team member posts comment, owner sees it and clicks "확인" → strikethrough

## Context
- Comment button class: .task-comment-btn, panel: .task-comment-panel #cmt-{taskId}
- JS functions: toggleComments(), loadComments(), sendComment(), confirmComment()
- _meLogin cached after first /api/me call
- All comment APIs require auth (session cookie)
