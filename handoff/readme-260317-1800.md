# Handoff - daily-focus - 2026-03-17 18:00

## Completed
- Calendar full-month activity dots now load on page init
  - Added `loadCalendarActivity()` call to `initApp()` so dots appear immediately on page open
  - `/api/calendar` endpoint returns full month done tasks + CC activity per day
  - `calActivityMap` populated on load; dot condition: `done > 0 || cc > 0`
  - Blue circle = `serverToday` (server KST), not client local date
  - Verified DB data: agent_activity 165~263 events/day for Mar 10–16; done tasks Mar 7–16
- All localStorage → server DB migration (completed in prior session)
- Telegram notifications + EOD report (completed in prior session)
- Project health score (completed in prior session)

## Remaining
- @mention feature (deferred)
- CommentPanel unification (deferred)
- `sync_workspace_projects()` overwrites custom folder_path (identified, not fixed)

## Next Steps
- Open http://localhost:8001 and verify calendar shows dots for Mar 7–16
- Verify blue circle is on Mar 17 (server KST today)
- If `sync_workspace_projects` bug needs fixing: every `build_day_snapshot()` call in app.py overwrites custom folder paths with auto-detected ones

## Context
- DB: `data/focus.db` (not `daily_focus.db`)
- Calendar data flow: `loadWeekTaskMap()` (week view) + `loadCalendarActivity()` (full month dots)
- `calActivityMap = { 'YYYY-MM-DD': { done: N, cc: N } }` used by `renderCalendar()`
- `serverToday` set from both `/api/week` and `/api/calendar` responses
- Server auth middleware blocks unauthenticated API calls (needs browser session cookie)
