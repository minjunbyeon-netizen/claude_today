# Handoff - daily-focus - 2026-03-18 00:40

## Completed - 50 Scenario Check + Fixes

### Bugs Fixed (7)
1. S-22~24: 서버 소유자 확인 추가 — PATCH/DELETE /api/requests/{id} + DELETE /api/requests/{id}/replies/{id} → created_by 비교, 다른 사람이면 403 반환
2. S-11: 내 댓글에만 × 버튼 표시 — isMine 조건 추가 (meLogin !== '익명' && created_by === meLogin)
3. S-49: saveDetail 성공/실패 피드백 — 버튼이 "저장 중..." → "저장됨" 1.2초 표시
4. S-50: deleteRequest/saveDetail에서 403/404 응답 처리 → showToast 오류 메시지
5. S-21: 30초 폴링 추가 — 포커스 입력창 있을 때 / 모달 열려있을 때 스킵
6. showToast() 함수 추가 — info/warn/error 색상 구분, bottom center 위치
7. deleteReply에도 403 처리 추가

### Still Noted (Not Fixed - Design Decisions)
- S-28: 동시 수정 race condition (last-write-wins) — 소규모 팀이라 허용 범위
- S-30: meLogin='익명' 시 mine 구분 불가 — GitHub OAuth 정상 로그인 전제
- S-34: 알람이 전체 공유됨 — 설계 결정 (쌍방향 협업 알람)
- S-38: 텍스트 길이 제한 없음 — DB TEXT, 실용적 영향 낮음

### 50-Scenario Summary
- OK: 36개
- Fixed: 7개
- Design decision / accepted: 7개

## Context
- 소유자 확인 로직: ALLOWED_GITHUB_USERS 첫 번째 계정은 관리자로 항상 수정/삭제 가능
- created_by가 빈 문자열이거나 login이 빈 문자열이면 제한 없이 통과 (레거시 데이터 호환)
- 폴링: setInterval 30000ms, _pollTimer로 중복 방지
