# Handoff - daily-focus - 2026-03-16 17:30

## Completed
- Added Google OAuth login to app.py (SessionMiddleware + auth_middleware + /login /auth/google /auth/callback /logout routes)
- Created static/login.html (minimal login page with Google button)
- Added httpx and itsdangerous to requirements.txt
- Created .env.example with all required env vars documented

## Remaining
- Google Cloud Console OAuth credentials not yet created (user must do this)
- Droplet env vars not yet set (user must SSH and set them)
- Not yet pushed/deployed to Droplet

## Next Steps
1. Go to console.cloud.google.com → create OAuth 2.0 credentials
2. Add authorized redirect URI: http://<droplet-ip>:8001/auth/callback (or domain)
3. SSH into Droplet → set env vars in systemd service or /etc/environment
4. git push → auto-deploys via GitHub Actions

## Context
- Droplet IP: 137.184.82.157 (from GitHub Actions workflow)
- GitHub Actions auto-deploys to /opt/daily-focus on push to master
- SessionMiddleware added LAST in app.py (outermost = runs first = session available in auth_middleware)
- ALLOWED_EMAILS env var: comma-separated list; if empty, any Google account can log in
- BASE_URL env var must match the OAuth callback URL registered in Google Cloud Console
