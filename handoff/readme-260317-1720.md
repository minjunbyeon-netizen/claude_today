# Handoff - daily-focus - 2026-03-17 17:20

## Completed
- 모든 localStorage → 서버 DB 마이그레이션 완료
  - 신규: GET/POST /api/ui-prefs (settings 테이블 ui_prefs 키)
  - 신규: GET /api/user-activity (user_activity 테이블)
  - _uiPrefs 인메모리 캐시 + setUiPref()/getUiPref() 래퍼
  - 600ms 디바운스 자동 저장 (잦은 클릭 최적화)
  - 이전된 항목: active_tab, active_subtab, claude_open, acc_open, proj_order, card_states, wf_collapsed, wfg_states
  - localStorage 참조 완전 제거
- user_activity 로그 테이블 신설
  - task_create / task_update / task_delete / wf_groups_save 이벤트 자동 기록
- 초기화 시퀀스 개선
  - loadUiPrefs() + loadWfGroupsFromServer() 병렬 await
  - applyUiPrefs()로 탭/패널/아코디언/WF 상태 일괄 복원

## Remaining
- @mention feature (deferred)
- CommentPanel unification (deferred)

## Context
- 어느 기기에서 접속해도 동일한 UI 상태 유지
- 서버 장애 시: _uiPrefs defaults(빈 값)로 안전하게 폴백
- /api/user-activity?limit=N 으로 최근 액션 조회 가능
