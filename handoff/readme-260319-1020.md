# Handoff - daily-focus - 2026-03-19 10:20

## Completed
- 삭제: keepalive.py (30초 헬스체크 루프 — PM2와 중복)
- 삭제: watchdog.py (60초 포트 감시 재시작 — PM2와 중복)
- 삭제: background_start.bat (keepalive 전용 배치)
- 삭제: Windows Startup "Daily Focus Background.vbs" (keepalive 시작 트리거)
- git commit + push 완료 (commit 5079e26)

## Remaining
- None

## Next Steps
- PM2만으로 daily-focus 운영 — `pm2 restart daily-focus` 로 재시작
- 터미널 창 깜빡임 재발 시 확인: `pm2 logs daily-focus` + wmic process 확인

## Context
- 원인: PM2(autorestart) + keepalive(30s) + watchdog(60s) 세 개가 포트 8001을 동시에 관리
- PM2가 1449회 재시작한 근본 원인은 keepalive/watchdog이 추가 uvicorn을 띄워 포트 충돌
- 이제 PM2 단독으로 uvicorn 관리: `script: .venv/Scripts/python.exe`, `args: -m uvicorn app:app --host 0.0.0.0 --port 8001`
- zombie-watchdog (PM2 항목)은 별개 — Claude Code 감시용 runner.py, 건드리지 않음
