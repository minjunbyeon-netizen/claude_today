# Handoff - daily-focus - 2026-03-12 14:30

## Completed
- Sessions panel now shows ALL C:/work folders (not just DB/JSONL-reported ones)
  - 3rd pass: direct C:/work scan adds missing folders as "idle" status
  - JSONL scan now filters to only C:/work-existing folders (prevents deleted folders reappearing)
  - Deleted stale DB entries (daily-check-stage-*, migration, etc.)
- Added `idle` dot CSS and JS display logic (shows "대기 중" instead of huge minutes_ago)
- C:/work cleanup: 29 -> 18 folders (10 staging/backup/old-version folders deleted)

## Remaining
- None

## Next Steps
- Visual verification: open http://localhost:8001 and confirm sessions panel shows 17 entries

## Context
- Skip list for work scan: log, setting, config, data, tools (infrastructure folders)
- DB has 14 active projects; 3 idle (daily-focus-android, money-check, vps-setup)
- JSONL scan requires project to exist in C:/work to prevent ghost entries from deleted folders
