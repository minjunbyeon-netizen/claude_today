# Handoff - daily-focus / mcp VPS debug - 2026-03-13 13:55

## Completed
- Full debug of VPS mcp app at http://137.184.82.157/mcp/
- Fixed [WARN] blog_pull run_crawler.py import failure (No module named 'run_crawler') by adding `web/` dir to sys.path in app.py
- Installed missing `truststore` package into venv
- Committed fix to git, pushed, pulled on VPS, restarted mcp.service
- Verified all 6 API endpoints + 3 static files return 200 via nginx

## Remaining
- None critical. INFO log about Google OAuth missing is expected (SSO intentionally disabled, auto-login to dev@localhost instead)

## Next Steps
- If Google OAuth is needed: add GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET to /home/dev/work/mcp/.env
- Monitor /home/dev/work/mcp/logs/web-app.err.log for OSError port conflicts (caused by systemd Restart=always during previous bad state; now stable)

## Context
- Service: mcp.service (systemd), runs as user 'dev', WorkingDirectory=/home/dev/work/mcp
- Port: 5051, nginx subpath: /mcp/
- Venv: /home/dev/work/mcp/.venv
- Root cause of blog_pull WARN: app.py line 148 added blog_pull/ to sys.path but NOT web/ where run_crawler.py lives; the run_crawler.py imports bs4 which was available in venv but module couldn't be found first
- Fix: added `sys.path.insert(0, str(Path(__file__).parent))` at line 149 in web/app.py
- truststore was in requirements.txt but not installed; installed manually with pip
- Memory: 744Mi/961Mi used (tight but functional), Disk: 8.3G/24G used (36%)
