# Handoff - daily-focus - 2026-03-16

## Completed
- Added "작업 폴더" sidebar section showing ALL C:/work folders with status dots and CC buttons
- Folders sorted: active (start/step) → done → stale → idle
- CC button on each folder row opens Claude Code for that project
- Collapse/expand toggle with localStorage persistence (`wf_collapsed`)
- Sessions panel (CC/VIEW Sessions) now filters out pure idle work-scan entries — only shows truly active/recent sessions

## Remaining
- None

## Next Steps
- Reload page at localhost:8001 to see new "작업 폴더" section at the bottom of sidebar
- Status dots: blue=start, yellow=step, green=done, gray=stale/idle

## Context
- Data source: `/api/agent-status` already returns all C:/work folders (with `from_work_scan: true` for idle ones)
- Skip list in backend: `{'log', 'setting', 'config', 'data', 'tools'}` — these won't appear in the list
