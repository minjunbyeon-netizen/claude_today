# Handoff - daily-focus - 2026-03-16 18:10

## Completed
- GitHub OAuth login fully working (local LAN + droplet)
- All redirect URLs use BASE_URL path prefix — works for both local and droplet subpath
- login.html button uses relative path `auth/github` (not absolute)
- LOCAL .env file created with GITHUB credentials, BASE_URL=http://192.168.1.100:8001
- ALLOWED_GITHUB_USERS set to: minjunbyeon-netizen, hipup500-afk (both local .env and droplet)
- SECRET_KEY fixed in both environments (sessions persist across restarts)
- /health endpoint added (no auth required) — keepalive now checks /health instead of /api/today
- Terminal popup loop fixed: keepalive was restarting because /api/today returned 401
- app.py loads .env automatically at startup (no python-dotenv dependency)
- 11-scenario debug sweep: all pass

## Remaining
- None

## Next Steps
- If team member login still fails, check GitHub OAuth App callback URL is set to: http://192.168.1.100:8001/auth/callback
- Droplet is accessible at http://137.184.82.157/daily-focus/ (separate data, for remote access)
- Local LAN sharing at http://192.168.1.100:8001/

## Context
- Local BASE_URL = http://192.168.1.100:8001 (for OAuth redirect_uri to match GitHub app setting)
- Droplet BASE_URL = http://137.184.82.157/daily-focus (set in /opt/daily-focus/.env)
- _BASE_PATH extracted from BASE_URL.path — empty locally, "/daily-focus" on droplet
- keepalive.py health check: GET /health → 200 (no auth)
- Server started via: powershell Start-Process hidden uvicorn on port 8001
- itsdangerous installed in local venv (required for SessionMiddleware)
