# Handoff - daily-focus - 2026-03-17 15:30

## Completed
- CC button launch fully debugged and fixed (commit 05f620a)
  - Tested all attempted approaches — all failed in pm2 subprocess context:
    - Direct Popen of App Execution Alias stub (WindowsApps/wt.exe): silently exits 0, does nothing
    - Start-Process wt in PowerShell: 0x80070002 (alias not resolved without user shell context)
    - os.startfile("wt: ..."): URI handler not registered → "앱을 다운로드하여 이 wt 링크를 여세요" dialog
    - cmd /c start "" wt ...: rc=1 access denied
  - Root cause: App Execution Alias stubs ONLY work via Windows Shell activation mechanism
  - Final fix: get real wt.exe path at runtime via Get-AppxPackage InstallLocation, Popen directly
    - Path: C:\Program Files\WindowsApps\Microsoft.WindowsTerminal_1.23.20211.0_x64__8wekyb3d8bbwe\wt.exe
    - Real binary uses IPC to communicate with running WT → new window opens, wt.exe exits rc=0
  - Verified via Python subprocess test: poll=0, WT window title changed from Claude Code → new-window
- pm2 restarted with final fix

## Remaining
- @mention feature (MAJOR — deferred)
- CommentPanel unification across 4 views (deferred)
- End-of-day report auto-generation
- Telegram briefing integration
- Project health score

## Next Steps
- User should press CC button on any project card and verify WT opens in correct directory
- If Get-AppxPackage returns empty (edge case: WT not installed properly), error message shown
- Focus on other features

## Context
- Real wt.exe is NOT the stub at C:\Users\USER\AppData\Local\Microsoft\WindowsApps\wt.exe
- Real wt.exe is at the AppxPackage InstallLocation (dynamically discovered at call time)
- Windows Terminal IPC: when WT is already running, calling wt.exe new-window sends IPC command
  and the wt.exe process exits immediately with rc=0 — this is correct behavior, not a failure
- Previous launchBriefItem object bug also fixed (commit f4a00a4)
