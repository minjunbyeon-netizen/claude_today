# Handoff - daily-focus - 2026-03-19 15:30

## Completed
- Investigated DigitalOcean droplet as possible cause of terminal window flashing
- Confirmed deploy.yml SSHes only to droplet (137.184.82.157), never touches local machine
- Confirmed no active git hooks, no webhook receiver endpoints in app.py
- Confirmed remote_url API only sends data from local to droplet, not the other way
- All terminal flashing root causes were local: keepalive.py + watchdog.py + PM2 triple conflict + venv launcher proxy orphan issue (all fixed in previous sessions)

## Remaining
- None

## Next Steps
- Monitor PM2 logs for any new restarts: `pm2 logs daily-focus`
- Reboot test recommended to verify pm2-windows-startup auto-start works correctly

## Context
- deploy.yml: push to master → GitHub Actions SSHes to 137.184.82.157 → git pull + systemctl restart on DROPLET only
- remote_url in app.py: local pushes claude-usage stats TO droplet, not bidirectional
- Current fix: PM2 runs system Python directly (C:/Users/USER/.../Python312/python.exe) + PYTHONPATH injection for venv packages
- PM2 status: single process, 0 restarts, port 8001 stable
- pm2-windows-startup registered in HKCU\Run for boot persistence
