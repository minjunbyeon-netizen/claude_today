# Handoff - daily-focus - 2026-03-11 15:30

## Completed
- Added "↻ Git 감지" button in task panel (index.html line ~1608) next to existing sync button
- Added `syncGit()` async JS function that calls POST /api/sync-git and shows result in syncMsg
- Fixed sync-git backend to also scan `carried_over` status tasks (was only scanning `todo`)
- Confirmed /api/sync-git backend was already fully implemented (scans repos, marks done on commit)

## Remaining
- Test the sync-git button live: create a commit on a matching repo and press "Git 감지"
- Consider adding auto-trigger: call syncGit() during autoRefreshTick() every N minutes
- "이어서:" task matching may need tuning — currently only matches `[project]` prefix tasks

## Next Steps
1. Restart server: `python run.py` or `start.bat`
2. Open http://localhost:8001
3. Press "↻ Git 감지" button and verify it scans repos and shows result
4. Check task with `[project-name] 이어서:` title gets auto-completed after a commit

## Context
- Backend: /api/sync-git at app.py:2586 — scans REPO_SCAN_ROOTS, matches by project name normalization
- Frontend: index.html — button at ~line 1608, JS at syncGit() function (~line 2671)
- Matching logic: repo basename vs `[project]` prefix in task title (normalize by removing -/_)
- Port: 8001 (defined in runtime_config.py, started via start.bat or run.py)
