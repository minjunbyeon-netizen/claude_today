# Handoff - daily-focus - 2026-03-19 18:00

## Completed
- 각 폴더마다 "북극성 한 줄" 입력/표시 기능 추가
- 데이터: group.folderMeta[folderName].northStar 에 저장 (wf-groups API 재사용, 별도 API 없음)
- 설정 모달: 폴더 칩 → wf-folder-list 리스트로 변경 (폴더명 + 북극성 입력 + 삭제 버튼)
- 메인 화면: grp-folder-hd 아래 .grp-folder-star 로 표시 (설정된 경우에만)
- setFolderNorthStar(gid, folder, text) 함수 추가

## Remaining
- None

## Next Steps
- 설정 ⚙ 열기 → 각 그룹 내 폴더 이름 옆 입력란에 북극성 입력 → 저장 즉시 반영
- 메인 화면 폴더 헤더 아래 회색 소문자로 북극성 표시

## Context
- folderMeta is stored inside each group object in wf-groups JSON
- No backend change needed — folderMeta is part of the groups payload
- .grp-folder-star CSS: 11px #6E6E73, padding 0 16px 6px 16px, only renders if northStar is set
- .wf-folder-list replaces .wf-folder-chips in settings modal
