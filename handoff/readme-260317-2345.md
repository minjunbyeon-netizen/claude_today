# Handoff - daily-focus - 2026-03-17 23:45

## Completed
- requests.html: 삭제 확인 커스텀 모달 추가 (요청 삭제 + 댓글 삭제 모두 적용)
  - scale+fade 애니메이션, "취소" / "삭제" 버튼, 제목에 삭제 대상 이름 표시
- requests.html: 전송 버튼 레이아웃 오버플로 수정 — absolute → flex row (align-items:flex-end)
- index.html: _setFutureMode(false) 복구 시 dataset.prevDisplay로 원래 상태 복원
- index.html: escHtml 중복 함수를 arrow function const로 통일
- deleteReply에도 확인 모달 추가 (기존에는 confirm 없이 바로 삭제됨)

## 10-Point Conflict Check Results
1. escHtml vs esc 중복 → const alias로 통일 (FIXED)
2. _setFutureMode restore 시 원래 display 유실 → dataset.prevDisplay로 복구 (FIXED)
3. deleteReply confirm 없음 → 모달 추가 (FIXED)
4. 전송 버튼 overflow → flex layout (FIXED)
5. date_alarms CREATE IF NOT EXISTS in init_db → 기존 DB에 서버 재시작 시 자동 생성 (OK)
6. requests.html /api/ 하드코딩 → 로컬 전용, BASE_PATH 변경 시 주의 필요 (NOTED)
7. initApp loadTodayAlarms → tabWork 상태여도 DOM은 있으므로 OK
8. loadTodayByDate(ds) 미래 날짜에서도 호출 → API 응답 빈 배열, 무해 (OK)
9. ensure_base_schema 실행 순서 → init_db → ensure_base_schema 정상 (OK)
10. autoResize on dynamic textarea → renderDetail 후 즉시 호출 (OK)

## Remaining
- None

## Context
- Custom delete modal: showDelModal(title, desc, callback) / closeDelModal() / confirmDelModal()
- Modal backdrop click → closes modal
- ESC key not wired (could add if needed)
