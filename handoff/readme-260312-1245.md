# Handoff - daily-focus - 2026-03-12 12:45

## Completed
- Fixed open-claude 0x80070002 error (root cause: App Execution Alias limitation)
  - `wt.exe` in `AppData\Local\Microsoft\WindowsApps\` is an App Execution Alias
  - Direct subprocess.Popen to wt.exe fails from background process context
  - Fix: use `cmd /c "wt new-window -d <path> -- cmd /k <claude_cmd>"`
  - cmd.exe has shell PATH access → wt alias works correctly
- 10/10 scenarios PASS (email-persona, gangseo-website, config non-git, lotto-app desktop path, Korean/English/special-char prompts, long prompt truncation, error cases)

## Debug Results
| # | Scenario | Result |
|---|---------|--------|
| 1 | daily-focus (git repo) | PASS |
| 2 | email-persona (original error) | PASS |
| 3 | gangseo-website (reported error) | PASS |
| 4 | config (is_repo=0, non-git workspace) | PASS |
| 5 | lotto-app (Desktop path) | PASS |
| 6 | Korean prompt | PASS |
| 7 | English prompt | PASS |
| 8 | Special chars in prompt | PASS |
| 9 | Nonexistent project → error | PASS |
| 10 | Long prompt (>1200 chars, truncated) | PASS |

## Key Fix
```python
wt_cmd = 'wt new-window -d "%s" -- cmd /k %s' % (target_path, claude_command)
subprocess.Popen(["cmd", "/c", wt_cmd], shell=False, env=env, creationflags=CREATE_NO_WINDOW)
```

## Remaining
- Visual verification: click "계속" button and confirm WT window appears maximized in foreground

## Context
- App Execution Aliases (WindowsApps\wt.exe) cannot be spawned directly from background processes
- cmd.exe /c resolves the alias correctly via shell PATH
- Force-focus: AttachThreadInput trick in PowerShell (runs after 800ms delay)
